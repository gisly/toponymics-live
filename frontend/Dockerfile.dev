FROM node:20-alpine

WORKDIR /app

# Сначала только манифест, чтобы кешировать npm install
COPY package.json package-lock.json* ./
RUN npm install

# Код монтируется через volume в dev
COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
